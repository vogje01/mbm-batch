# Housekeeping for the batch tables
consolidation:
  batch:
    # Job execution infos
    jobExecutionInfo:
      entityActive: true
      houseKeepingDays: 20
      chunkSize: 1000
    # Job execution log
    jobExecutionLog:
      entityActive: true
      houseKeepingDays: 20
      chunkSize: 1000
    # Step execution log
    stepExecutionInfo:
      entityActive: true
      houseKeepingDays: 20
      chunkSize: 1000
