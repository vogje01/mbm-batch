# Basis components
# Consists of: users, client, userRole, clientRoleOwnership, functionalUnit, roleFunctionalUnit, userAuthorization,
#              securityOrganization, clientAuthorization, trustworthExclusion, message
# fullSync: last_change on received entities is neglected.
# cutOffDays: number of days to synchronize, 0 means all.
# chunkSize: number of entities to process per chunk.
# kafka.active: is only needed when MySQL -> Db2 sync is necessary.
# kafka.topic: kafka topic to write to.
# kafka.partitions: number of kafka partitions.
# kafka.replicas: number of kafka replicas.
dbSync:
  basis:
    client:
      entityActive: false
      fullSync: false
      cutOffDays: 2
      chunkSize: 1000
      kafka:
        active: false
        topic: client
        partitions: 1
        replicas: 1
    users:
      entityActive: false
      fullSync: false
      cutOffDays: 2
      chunkSize: 1000
      kafka:
        active: false
        topic: users
        partitions: 1
        replicas: 1
    userRole:
      entityActive: false
      fullSync: false
      cutOffDays: 2
      chunkSize: 1000
      kafka:
        active: false
        topic: userRole
        partitions: 1
        replicas: 1
    clientRole:
      entityActive: false
      fullSync: false
      cutOffDays: 2
      chunkSize: 1000
      kafka:
        active: false
        topic: clientRole
        partitions: 1
        replicas: 1
    functionalUnit:
      entityActive: false
      fullSync: false
      cutOffDays: 2
      chunkSize: 1000
      kafka:
        active: false
        topic: functionalUnit
        partitions: 1
        replicas: 1
    userAuthorization:
      entityActive: false
      fullSync: false
      cutOffDays: 2
      chunkSize: 1000
      kafka:
        active: false
        topic: userAuthorization
        partitions: 1
        replicas: 1
    securityOrganization:
      entityActive: false
      fullSync: false
      cutOffDays: 2
      chunkSize: 1000
      kafka:
        active: false
        topic: securityOrganization
        partitions: 1
        replicas: 1
    clientAuthorization:
      entityActive: false
      fullSync: true
      cutOffDays: 2
      chunkSize: 1000
      kafka:
        active: false
        topic: clientAuthorization
        partitions: 1
        replicas: 1
    trustworthExclusion:
      entityActive: false
      fullSync: false
      cutOffDays: 2
      chunkSize: 1000
      kafka:
        active: false
        topic: trustworthExclusion
        partitions: 1
        replicas: 1
    message:
      entityActive: false
      fullSync: false
      cutOffDays: 2
      chunkSize: 1000
      kafka:
        active: false
        topic: message
        partitions: 1
        replicas: 1
