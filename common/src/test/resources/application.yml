spring:
  main:
    banner-mode: off
  datasource:
    platform: h2
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
